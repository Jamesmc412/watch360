WATCH 360


Updated HTML with CSS Changes to change the color from blue to green
	- changed background to black




server:
downloaded XAMPP
start the server (Apache and MySQL) using the control panel 

Access phpMyAdmin:
Open your web browser and go to http://localhost/phpmyadmin.

Create the Database
Create the Users Table: execute the following SQL command in the SQL tab

create folder in server's root directory C:\xampp\htdocs for XAMPP

then put created files in the folder

Update Database Connection in config.php

Test Your Application
Open Your Browser:
Go to http://localhost/watch360/register.php to access the registration page.